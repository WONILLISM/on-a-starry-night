import styled from "styled-components";

const NavigationBar = () => {
  return <NavStyle>NavigationBar</NavStyle>;
};

const NavStyle = styled.nav`
  margin-left: auto;
`;

export default NavigationBar;
